if game_mode = "tetris" {
	game_mode = "zelda";
	//if(camera){camera_destroy(camera)}
	//camera = camera_create_view(0, 0, 1920, 1080, 0, -1, -1, -1, 32, 32);
	//view_set_camera(0, camera);
	
}
else {
	game_mode ="tetris";
	////camera_destroy(camera);
	//camera = camera_create_view(0, 0, 384, 216, 0, -1, -1, -1, 32, 32);
	//view_set_camera(0, camera);

}