/*

Spawning blocks!

	When ready to spawn, define variable as a random puzzle piece, then create the new instance.

		{
		var spawn = choose(puz_1,puz_2)

		instance_create_depth(880,0,depth,spawn)
		}

Move blocks by adding or subtracting 160?

Rotate pieces 



*/

image_angle